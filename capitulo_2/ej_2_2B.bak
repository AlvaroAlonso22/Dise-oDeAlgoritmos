#lang racket
  ;; Alice version tag
(define (alice-make-box w h x y)
  (list 'alice-box (cons w h) x . y))

(define (alice-area box)
  (let ((pair (cadr box)))
    (cdr pair))) 

;; Bob version  tag
(define (bob-make-box w h x y)
  (list 'bob-box (cons w h) x . y))

(define (bob-area box)
  (let ((pair (cadr box)))
    (* (car pair) (cdr pair))))  

